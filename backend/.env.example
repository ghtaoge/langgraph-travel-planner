# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# LangGraph Travel Planner — 环境变量配置
# 复制此文件为 .env 并填写实际值: cp .env.example .env
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── LLM (必填) ──
# 支持 DeepSeek / Qwen / OpenAI 等 OpenAI SDK 兼容的 API
LLM_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=your-api-key-here      # ← 必填: 你的 API Key
LLM_TEMPERATURE=0.7

# ── PostgreSQL (必填) ──
# 首次启动后端会自动创建业务表 (users, conversations, chat_messages)
# LangGraph AsyncPostgresSaver / PostgresStore 也会自动创建各自的管理表
#
# 使用 docker-compose 自建 PG (默认):
POSTGRES_URI=postgresql://travel_user:travel_pass_2026@localhost:5432/travel_planner
#
# 使用外部 PG 实例时修改为你的连接串, 例如:
# POSTGRES_URI=postgresql://postgres:yourpassword@your-host:5432/your_database

# ── JWT Auth (必填) ──
# 生产环境务必使用随机密钥, 不要使用默认值!
JWT_SECRET_KEY=change-me-in-production-use-a-random-string  # ← 必改!
JWT_EXPIRE_HOURS=24
JWT_ALGORITHM=HS256

# ── LangSmith (可选) ──
# 不填则关闭追踪
# LANGSMITH_API_KEY=your-langsmith-key
# LANGSMITH_PROJECT=langgraph-travel-planner
# LANGCHAIN_TRACING_V2=false

# ── Server ──
HOST=0.0.0.0
PORT=8000
